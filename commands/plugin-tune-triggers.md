---
description: Sharpen a plugin's skill trigger descriptions so each fires exactly when it should
argument-hint: "[plugin-dir]"
---
Use the **plugin-tune-triggers** skill on the plugin at $ARGUMENTS (default: current directory): gather the trigger surface, build a should/shouldn't-trigger test matrix, rewrite failing descriptions, and show the before/after diff with char counts and the matrix cases fixed.
