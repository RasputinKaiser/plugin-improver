---
description: Package and publish a plugin to the Claude Code and Codex marketplaces
argument-hint: "[plugin-dir]"
---
Use the **plugin-release** skill to package and publish the plugin at $ARGUMENTS (default: current directory) to both marketplaces: verify a clean tree and green validator, confirm agreeing versions, update the changelog, refresh both marketplace manifests, and prepare the git tag. Prepare everything and hand the final tag, push, and publish to me — never run them yourself.
